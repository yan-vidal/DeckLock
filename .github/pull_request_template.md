Describe the problem and resulting behavior.

Validation:
- Regression added (or why no behavior test is needed):
- `scripts/check` / `scripts/check --all` result and tested commit:
- Manual/device checks and remaining limits:

If changing tests, CI, security boundaries, configuration compatibility or release
behavior, explain the contract change and any coverage removed.
